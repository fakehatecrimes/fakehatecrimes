%form{action: "/media", method: "post"}
  = hidden_field_tag('user_id', (current_user ? current_user.id : nil))
  = hidden_field_tag('authenticity_token', form_authenticity_token)

  = render partial: 'new', locals: {what: 'medium', medium: @medium}
